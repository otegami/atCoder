# i, j, k = gets.strip.split.map(&:to_i)
# N = i + j + k
# N.times { |i| numbers << i }
# numbers.permutation.each do |n|
#   a = n.shift(i)
#   b = n.shift(j)
#   c = n.shift(k)
#   i, j, k = 0, 0, 0
#   while i < a.length
    
#     if a[i-1] < a[i]
#       while j < b.length

#     else
#       i + 1
#     end
#   end
# end